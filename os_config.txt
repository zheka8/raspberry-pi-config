# RPI config
sudo apt install raspi-config
sudo apt autoremove

# RPI settings
# setup wireless
# hostname
# password
# timezone

# check packages by size
dpkg-query -Wf '${Installed-Size}\t${Package}
' | sort -n

# remove unwanted large packages
sudo apt purge -y wolfram-engine
sudo apt purge -y libreoffice*
sudo apt autoremove
sudo apt clean

# update packages and OS
sudo apt update
sudo apt full-upgrade
sudo apt clean

# install rclone
curl -L https://raw.github.com/pageauc/rclone4pi/master/rclone-install.sh | bash
https://medium.com/@artur.klauser/mounting-google-drive-on-raspberry-pi-f5002c7095c2

# setup a cronjob for rclone
# move contents of images to gdrive folder images
rclone move images gdrive:images
# sync the logs
rclone sync -v camera.log gdrive:logs

# setup eth0 for wired connectivity
# edit /etc/dhcpcd.conf
# 192.168.0.10 / 24
interface eth0
static ip_address=192.168.0.10/24
static routers=192.168.0.1
